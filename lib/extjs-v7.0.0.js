const VERSION = "v7.0.0.156";

require( "../share/" + VERSION + "/ext.css" );
require( "../share/" + VERSION + "/css-vars.js" );
const Ext = require( "../share/" + VERSION + "/ext.js" );

module.exports = Ext;

// overrides
// require( "./" + VERSION + "/test.js" );
