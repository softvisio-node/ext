const VERSION = "v7.0.0";
const BUILD = "v7.0.0.156";

require( "../share/" + BUILD + "/ext.css" );
require( "../share/" + BUILD + "/css-vars.js" );
const Ext = require( "../share/" + BUILD + "/ext.js" );

module.exports = Ext;

// overrides
// require( "./" + VERSION + "/overrides.js" );
